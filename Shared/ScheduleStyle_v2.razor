@code {
    [Parameter] public int gameCount { get; set; }
    }
<style>
    /*The 'Week [X] Schedule' title at the top of the screen*/
    .scheduleTitle {
        position: sticky;
        text-align: center;
        margin-bottom: 2%;
        display: flex;
        padding: 0; /*override CSS framework*/
    }

    .content {
        padding: 0.5rem !important; /*override CSS framework*/
    }

    /*Arrow icons to move to another week*/
    .weekNavigator {
        cursor: pointer;
        font-size: 3em;
    }

    .weekTitle {
        padding: 0;
        vertical-align: middle;
        font-size: 2rem;
        font-family: 'OldSportAthletic';
    }

    /*Home, Away, Spread headers*/
    .scheduleHeaders {
        background: radial-gradient(#5B7B5C, black);
        color: white;
        padding: 10px;
        padding: 1%;
        /*border: 1px solid white;*/
        border-radius: 50px;
        margin-top: 1%;
        text-align: center;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .scheduleHeaders:hover {
            transition: ease-in-out 0.4s;
            transform: scale(0.95);
        }


    .spread, .datetime {
    }

    .spread {
        font-size: 3vw;
    }

    .datetime {
        font-size: 1vw;
        overflow-wrap: break-word;
    }

    /*Hide the radio buttons*/
    [type=radio] {
        opacity: 0;
        width: 0;
        height: 0;
    }

    /*Set the team name to the top left corner of its container div, place it above the other elements*/
    .teamnameimg {
        position: absolute;
        z-index: 1;
        border-radius: 25%;
        padding: 2%;
        bottom: 0%;
        left: 3%;
        height: 25%; /*After standardizing the width of the source img's, this gives a nice 2:1 aspect ratio -- easy to read!*/
        width: 50%;
    }

    /*Win-loss labels*/
    .record {
        position: absolute;
        z-index: 1;
        right: 0;
        bottom: 0;
        padding: 5%;
        display: table-cell;
    }

    /*Grayscale images when the radio button is not checked*/
    [type=radio] + label img {
        -webkit-filter: grayscale(100%);
        filter: grayscale(100%);
    }

    /*Full-color images when the radio button is checked*/
    [type=radio]:checked + label img {
        -webkit-filter: grayscale(0%);
        filter: grayscale(0%);
    }

    .teampick {
        margin: 1%;
    }

        .teampick label {
            cursor: pointer;
            width: 100%;
            height: 100%;
        }

        .teampick input[type="radio"] ~ div {
            position: absolute;
            width: 100%;
            height: 100%;
            left: 0;
            display: initial;
            z-index: -1;
        }

        .teampick input[type="radio"]:checked ~ div {
            transform: scale(.95);
            transition: ease-in-out 0.4s;
            box-shadow: 2px 2px 20px rgba(0,0,0,0.4);
        }

        .teampick input[type="radio"]:checked ~ label .teamlogo {
            transform: scale(1.05);
        }

    /*Modify the logo*/
    .teamlogo {
        height: auto; /*Keeps aspect ratio intact*/
        width: 100%; /*Takes up the width of the label element it is in*/
        padding: 20%; /*Give some whitespace back to avoid collision with team name img and div border*/
        max-height: 510px; /*Prevent especially tall icons from irregularly large heights at wide resolutions*/
    }

        .teamlogo:hover {
            transform: scale(1.05);
            transition: ease-out 0.4s;
        }
</style>

